# OAIP Learning Platform

Платформа модульного обучения по основам алгоритмизации и программирования с автоматической проверкой заданий.

## 📚 Документация

- **[QUICK_START.md](QUICK_START.md)** - быстрый старт за 3 минуты
- **[PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - полная структура проекта
- **[ADDING_MODULES.md](ADDING_MODULES.md)** - как добавлять модули и задания
- **[README.md](README.md)** - этот файл (полное описание)

## Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **SQLite** - легковесная база данных
- **JWT** - аутентификация и авторизация
- **Pydantic** - валидация данных

### Frontend
- **React 18** - библиотека для создания UI
- **Vite** - сборщик проекта
- **React Router** - маршрутизация
- **Monaco Editor** - редактор кода (как в VS Code)
- **Axios** - HTTP клиент
- **Lucide React** - иконки

## Структура проекта

```
oaip-learning-platform/
├── backend/                  # Серверная часть
│   ├── app/
│   │   ├── models/          # Модели БД (User, Module, Task, Progress)
│   │   ├── schemas/         # Pydantic схемы для валидации
│   │   ├── api/             # API эндпоинты
│   │   ├── services/        # Бизнес-логика
│   │   └── utils/           # Утилиты (security, helpers)
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/                 # Клиентская часть
│   ├── src/
│   │   ├── pages/           # Страницы приложения
│   │   ├── services/        # API сервисы
│   │   └── styles/          # CSS стили
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## Функциональность

### Реализованные возможности

1. **Аутентификация и авторизация**
   - Регистрация новых пользователей
   - Вход в систему
   - JWT токены для защиты API

2. **Модули обучения**
   - Структурированные учебные модули
   - Теоретический материал
   - Отслеживание прогресса по модулям

3. **Задания**
   - Практические задания с описанием
   - Уровни сложности (easy, medium, hard)
   - Стартовый код для заданий
   - Тест-кейсы для проверки

4. **Редактор кода**
   - Monaco Editor с подсветкой синтаксиса
   - Автодополнение кода
   - Темная тема

5. **Автоматическая проверка кода**
   - Проверка синтаксиса Python
   - Запуск тест-кейсов
   - Детальные результаты проверки
   - Отображение ошибок

6. **Отслеживание прогресса**
   - Статистика по модулям
   - Статистика по заданиям
   - Общий прогресс обучения
   - История попыток решения

## Установка и запуск

### Требования
- Docker и Docker Compose
- Git

### Запуск через Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd oaip-learning-platform
```

2. Быстрый запуск с помощью скрипта:
```bash
chmod +x setup.sh
./setup.sh
```

Или вручную:

```bash
# Скопируйте .env файл
cp backend/.env.example backend/.env

# Запустите контейнеры
docker-compose up -d --build

# Заполните базу данных
docker exec oaip-backend python seed_data.py
```

3. Откройте в браузере:
   - Frontend: http://localhost:5173
   - Backend API: http://localhost:8000
   - API Documentation: http://localhost:8000/docs

4. Зарегистрируйте нового пользователя через интерфейс регистрации

### Первый запуск

После запуска база данных автоматически заполняется тестовыми модулями:
- Модуль 1: Переменные и типы данных (2 задания)
- Модуль 2: Условные операторы (2 задания)
- Модуль 3: Циклы (2 задания)

### Добавление новых модулей и заданий

Вы можете добавить новые модули через API Documentation (http://localhost:8000/docs) или отредактировать файл `backend/seed_data.py`.

Рекомендуемые модули для полного курса ОАиП:
1. ✓ Переменные и типы данных (реализовано)
2. ✓ Условные операторы (реализовано)
3. ✓ Циклы (реализовано)
4. Функции
5. Списки и массивы
6. Строки
7. Словари
8. Множества и кортежи
9. Работа с файлами
10. ООП основы

Для добавления новых модулей отредактируйте `seed_data.py` и выполните:
```bash
docker exec oaip-backend python seed_data.py
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получить текущего пользователя

### Модули
- `GET /api/modules/` - Список всех модулей
- `GET /api/modules/{id}` - Получить модуль по ID

### Задания
- `GET /api/tasks/module/{module_id}` - Задания модуля
- `GET /api/tasks/{id}` - Получить задание по ID
- `POST /api/tasks/submit` - Отправить решение на проверку

### Прогресс
- `GET /api/progress/stats` - Статистика прогресса

## Архитектура системы

### Backend
```
FastAPI Application
├── API Layer (Routers)
│   ├── Authentication
│   ├── Modules Management
│   ├── Tasks Management
│   └── Progress Tracking
├── Service Layer
│   ├── Auth Service (JWT)
│   ├── Code Checker (Execution & Testing)
│   └── Progress Tracker
├── Data Layer
│   └── SQLAlchemy Models
└── Database (SQLite)
```

### Frontend
```
React Application
├── Pages
│   ├── Login/Register
│   ├── Dashboard
│   ├── Module View
│   └── Task View (with Code Editor)
├── Services
│   └── API Client (Axios)
└── Components
    ├── Monaco Editor
    └── Progress Indicators
```

## Безопасность

- Пароли хэшируются с использованием bcrypt
- JWT токены для аутентификации
- CORS настроен для фронтенда
- Изолированное выполнение пользовательского кода

## Разработка

### Backend разработка
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### Frontend разработка
```bash
cd frontend
npm install
npm run dev
```

## Тестирование

Для запуска тестов:
```bash
# Backend
cd backend
pytest

# Frontend
cd frontend
npm test
```

## Масштабирование

Для продакшена рекомендуется:
1. Использовать PostgreSQL вместо SQLite
2. Настроить Nginx как reverse proxy
3. Использовать Redis для кеширования
4. Настроить мониторинг и логирование
5. Использовать Docker Swarm или Kubernetes

## Лицензия

MIT License

## Контакты

При возникновении вопросов создайте issue в репозитории.